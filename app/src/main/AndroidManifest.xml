<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell_pc.funtasticquiz">

    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity">

        </activity>
        <activity android:name=".Start" />
        <activity android:name=".Instruction" />
        <activity android:name=".About" />
        <activity android:name=".Exit" />
        <activity android:name=".Simple" />
        <activity android:name=".FinalResult" />
        <activity android:name=".SSound" />
        <activity android:name=".VVideo" />
        <activity android:name=".IImage"></activity>
    </application>

</manifest>